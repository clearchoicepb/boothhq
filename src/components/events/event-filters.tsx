/**
 * @deprecated Import from '@/components/events/filters' instead
 *
 * This file re-exports the EventFilters component and types for backward compatibility.
 * The component has been split into modular sub-components in the 'filters' folder.
 */

// Re-export main component and types for backward compatibility
export { EventFilters, type FilterState } from './filters'
